# Copy to .env and fill in before: docker compose up

SHEET_ID=your_google_sheet_id
SHEET_NAME=2026

# Inside Docker this path is overridden by docker-compose; keep for local dev.
GOOGLE_APPLICATION_CREDENTIALS=./credentials.json

PORT=3001
